[build]
builder = "DOCKERFILE"
dockerfilePath = "polla_futbolera/docker/Dockerfile"

[deploy]
startCommand = "docker/entrypoint.sh"
healthcheckPath = "/health/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
